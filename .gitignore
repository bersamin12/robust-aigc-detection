# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- Data: never commit imagery or derived banks ---
# Everything under data/ is acquired or generated: real datasets by
# scripts/acquire_data.py, synthetic fixtures by make_dummy_manifest.
# Ignore the whole tree rather than named subdirectories, so a stray
# `git add -A` can never commit gigabytes of imagery.
# Anchored to the repo root (leading slash): a bare `data/` also matches
# `src/aigcdet/data/` and `tests/data/` by basename at any depth, which
# silently hid new files added under those package directories (found while
# adding src/aigcdet/data/audit.py and tests/data/test_audit.py in Task 8;
# the pre-existing files there predate the rule that broke this).
/data/
*.parquet
*.npy
*.npz

# --- Feature banks and model artefacts ---
banks/
runs/
checkpoints/
*.pt
*.pth
*.safetensors
*.joblib

# --- Outputs that are regenerated, not authored ---
outputs/
figures/*.png
docs/contact_sheets/

# The data/, logs/ and runs/ symlinks point into ../experiments/, which is
# where every acquired image, feature bank, log and run output now lives (the
# repository holds only code). A pattern with a TRAILING SLASH matches a real
# directory and never a symlink, so `/data/` above leaves the `data` symlink
# reported as an untracked file -- one `git add -A` away from committing a
# link to a path that exists only on this machine. These slashless anchors
# cover both spellings.
/data
/logs
/runs

# --- Local scratch ---
scratch/
*.log
.DS_Store

# --- Subagent-driven-development scratch (ledger, briefs, review packages) ---
.superpowers/

# agent worktrees (transient, created by the Agent tool)
.claude/worktrees/

# --- Secrets: API keys are read from the environment, never committed ---
.env
*.env
!.env.example
